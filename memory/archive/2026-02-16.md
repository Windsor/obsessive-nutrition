# 2026-02-16 — Quiz Video Generator Mega Build Day

## Summary
Massive feature expansion of the Quiz Video Generator (192.168.68.139:5000). Added 10 new quiz video types, crop editor, category-grouped UI, and sample quizzes.

## Key Work Done

### Theme Music Fix
- Music files existed in `music/` but weren't registered in the `assets` DB table as `theme_music` type
- Copied to `assets/theme_music/` and registered 6 tracks in DB

### Eye Guess Quiz Type (new)
- Built `app/eye_guess_generator.py` — crops eye band from headshot photos
- Created "Guess the Actor's Eyes" quiz with 12 actors from Wikipedia
- Tightened crop from 20-45% to 28-40% of image height after Glenn's feedback
- Fixed: crop coords weren't being passed in routes.py `_generate_for_profile()`
- Fixed: apostrophe in "actor's" broke ffmpeg concat — now stripping quotes from slugs
- Fixed: intro/outro video assets weren't wired into eye_guess generator — added concat logic

### WYSIWYG Crop Editor
- Per-question visual crop editor with draggable/resizable rectangle
- Touch support for iPad
- Saves crop as percentages (0-100) to question's crop_x/y/w/h fields
- API endpoints: PUT/DELETE `/api/questions/<id>/crop`

### Quiz List UI — Grouped by Category
- `loadQuizzes()` now groups quizzes by category with collapsible section headers
- 8 rotating accent colors, quiz count badges

### 10 New Quiz Video Types Built
1. `zoom_guess` — Extreme center close-up → reveal
2. `flag_guess` — Country flags
3. `logo_guess` — Brand logos
4. `emoji_guess` — Emoji combo text → guess (no images)
5. `silhouette_guess` — Black shadow outline via threshold
6. `blur_guess` — 3-stage progressive Gaussian unblur
7. `scene_guess` — Movie screenshots
8. `sound_guess` — Audio waveform visualizer + clip mixing
9. `before_after_guess` — Then vs Now side-by-side
10. `odd_one_out` — Text grid, spot the different one

### Sample Quizzes Created
- Guess the Flag (15 countries)
- Guess the Movie from Emojis (12 movies)
- Guess the Logo (12 brands)
- Guess the Zoomed-In Animal (10 animals)
- Guess the Silhouette - Animals (10, reuses zoom images)
- Guess the Blurred Celebrity (10 celebs)
- Guess the Movie by Scene (10 movies)
- Cities Then & Now (5 cities, placeholder images)
- Odd One Out Challenge (10 text-based questions)

### Frame TV
- Restarted the dashboard LaunchAgent after Mac mini reboot
- Triggered manual update, running on 15-min cycle

## Technical Notes
- All generators follow same pattern: PIL frames → ffmpeg encode → optional music → optional intro/outro concat
- Batch 1 (5 types) and Batch 2 (5 types) built in parallel via sub-agents, merged routes after
- Some Wikimedia images rate-limited — ~6 placeholder images across quizzes
- `before_after_guess` needs real before/after photos uploaded manually
